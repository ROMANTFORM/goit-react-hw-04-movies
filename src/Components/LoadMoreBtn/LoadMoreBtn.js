
const LoadMoreBtn = ({onClick}) => {
    return (
        <button
            type="button"
            className="Load-more-btn"
            onClick={onClick}
        >Load More</button>
    )
};

export default LoadMoreBtn;